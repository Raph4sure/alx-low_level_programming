#include <stdio.h>

/**
 *main - Using main function;
 *
 *Description: This program print if the number is positive, zero, or negative
 *Return: 0
 */
int main(void)
{
	char d;

	for (d = 'z'; d >= 'a' ; d--)
	{
		putchar(d);
	}
	putchar('\n');
		return (0);
}
