#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 *main - Using main function;
 *
 *Description: This program print if the number is positive, zero, or negative
 *Return: 0
 */
int main(void)
{
	int i;

	for (i = 0; i < 10 ; i++)
	{
		printf("%i", i);
	}
	putchar('\n');
		return (0);
}
